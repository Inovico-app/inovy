import type { Metadata } from "next";
import SignInPage from "./sign-in-content";

export const metadata: Metadata = {
  title: "Sign In",
  description: "Sign in to your Inovy account",
};

export default SignInPage;
